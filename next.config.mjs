/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "aceternity.com",
      "unsplash.com",
      "images.unsplash.com",
      "plus.unsplash.com",
    ],
  },
};

export default nextConfig;
